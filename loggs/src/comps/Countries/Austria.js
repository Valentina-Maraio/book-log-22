import React from 'react'
import Button from 'react-bootstrap/Button';

const Austria = () => {
    return (
        <div className="d-grid gap-2">
            <Button size="lg" variant="warning">AUSTRIA</Button>{' '}
        </div>
    )
} 

export default Austria