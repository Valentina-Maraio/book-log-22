import React from 'react'
import Button from 'react-bootstrap/Button';

const Francia = () => {
    return (
        <div className="d-grid gap-2">
            <Button size="lg" variant="warning">FRANCIA</Button>{' '}
        </div>
    )
}

export default Francia 